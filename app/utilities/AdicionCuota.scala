package utilities

import scala.collection.mutable.ListBuffer
import models._

class AdicionCuota {
    def AgregarACuota(cuotas:List[Tabla], adiciones:List[Descuento], valor_inicial: BigDecimal, saldo_actual:BigDecimal, amortiza: Int):List[Adicion] = {
        var lista:ListBuffer[Adicion] = new ListBuffer[Adicion];
        lista.toList
    }
}